#!/bin/sh

set -e

# enable zarafa-webaccess-mobile for apache
if [ -x "`which a2ensite`" ]; then
  # no error, file may have been renamed
  a2ensite zarafa-webaccess-mobile || true
fi

#DEBHELPER#

exit 0
